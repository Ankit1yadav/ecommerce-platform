cloud:
  aws:
    credentials:
      use-default-aws-credentials-chain: true
    region:
      static: us-east-1
server:
  port: 8081
management:
  endpoints:
    web:
      exposure:
        include: health