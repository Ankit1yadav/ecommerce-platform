spring:
  profiles:
    active: aws
cloud:
  aws:
    credentials:
      use-default-aws-credentials-chain: true
    region:
      static: us-east-1
eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/
server:
  port: 8081