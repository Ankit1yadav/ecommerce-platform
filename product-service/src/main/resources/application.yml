spring:
  application:
    name: product-service
  jpa:
    hibernate:
          ddl-auto: update
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    enabled: false
server:
  port: 8081
management:
  endpoints:
    web:
      exposure:
        include: health, info