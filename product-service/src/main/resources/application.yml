spring:
  application:
    name: product-service
  profiles:
    active: local

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8081
management:
  endpoints:
    web:
      exposure:
        include: health, info